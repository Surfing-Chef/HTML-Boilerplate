This folder contains scss files that only offers functionality
if running a task manager or a SASS compiler.

Use main.css in the css directory if you are not using the
above-mentioned workflow.  Enter styles starting on line 97.

Be sure the links at the top of index.html in the root
directory are commented/uncommented appropriately.
